

Notes = Table(
   'notes', meta, 
   Column('note_id', Integer, primary_key = True), 
   Column('note_title', String), 
   Column('note_content', String),
   Column('note_creationDate', DateTime),
)

db.session.commit()